/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "img2.storyblok.com",
      "asset.kompas.com",
      "cdn.idntimes.com",
      "hipwee.com",
    ],
  },
};

export default nextConfig;
